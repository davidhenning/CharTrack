machine:
  environment:
    ANDROID_HOME: /home/ubuntu/android
dependencies:
  pre:
    - chmod +x install-dependencies.sh
  cache_directories:
    - ~/.android
    - ~/android
  override:
    - ./install-dependencies.sh